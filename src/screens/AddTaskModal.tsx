import React from "react";
import { View, Text } from "react-native";

const AddTaskModal = () => {
    return (
        <View>
            <Text>Modal</Text>
        </View>
    );
};

export default AddTaskModal;
